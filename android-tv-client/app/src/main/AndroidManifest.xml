<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Android TV feature - optional to support both TV and tablets -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- Explicitly declare TV compatibility -->
    <uses-feature
        android:name="android.hardware.type.television"
        android:required="false" />

    <!-- Touchscreen not required (supports TV remotes) -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <!-- HDMI-CEC support for power control (optional, for Android TV boxes) -->
    <uses-feature
        android:name="android.hardware.hdmi.cec"
        android:required="false" />

    <!-- Network permissions for API communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Auto-start permissions for digital signage -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Power management permissions for scheduled on/off -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- REMOVED: WRITE_SECURE_SETTINGS - Not allowed on Play Store -->

    <!-- Foreground service permission for boot auto-start (Android 9+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Notification permission for foreground service (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MesophySignage"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize"
            android:keepScreenOn="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Boot receiver for auto-start functionality -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <!-- Foreground service for reliable boot auto-start -->
        <service
            android:name=".BootStarterService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        
        <!-- Test receiver for power management testing -->
        <receiver
            android:name=".PowerTestReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.mesophy.signage.POWER_SCHEDULE_TEST" />
                <action android:name="com.mesophy.signage.FORCE_POWER_ON" />
                <action android:name="com.mesophy.signage.FORCE_POWER_OFF" />
                <action android:name="com.mesophy.signage.GET_POWER_STATUS" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
